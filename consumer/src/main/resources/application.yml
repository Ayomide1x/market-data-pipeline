spring:
  application:
    name: consumer
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:127.0.0.1:29092}
    consumer:
      group-id: ema-consumers
      auto-offset-reset: earliest

logging:
  structured:
    format:
      console: ecs
